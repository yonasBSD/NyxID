# Placeholder so the credentials/ directory exists in a fresh clone.
#
# Drop the following local files here (gitignored — see ../.gitignore):
#   asc-api-key.p8                  App Store Connect API key (.p8)
#   play-service-account.json       Google Play service account JSON
#
# See ../README.md for how to obtain these.
